// import { createSlice } from "@reduxjs/toolkit";
// import { TCookieConsentState } from "./interfaces";

// const initialState: TCookieConsentState = {
//   showCookie: null
// };

// const cookieConsentSlice = createSlice({
//   name: 'cookieModal',
//   initialState,
//   reducers: {
//     acceptCookie: (state) => {
//       state.showCookie = true;
//     },
//     rejectCookie: (state) => {
//       state.showCookie = false;
//     },
//     resetCookie: (state) => {
//       state.showCookie = false;
//     }
//   },
// });

// export const { acceptCookie, rejectCookie, resetCookie } = cookieConsentSlice.actions;
// export default cookieConsentSlice.reducer;